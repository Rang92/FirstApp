<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:onClick="pressWWT">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:hint="name"
        android:ems="10"
        android:id="@+id/nameID"
        android:layout_marginTop="58dp"
        android:layout_below="@+id/signID"
        android:layout_centerHorizontal="true"
        android:width="320dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/SendTitle"
        android:id="@+id/buttonID"
        android:onClick="pressSend"
        android:layout_marginStart="57dp"
        android:textColor="#ffffff"
        android:background="#000000"
        android:layout_below="@+id/ageID"
        android:layout_alignStart="@+id/ageID"
        android:layout_marginTop="77dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="@string/SignInTitle"
        android:id="@+id/signID"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="38dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/ageID"
        android:layout_below="@+id/nameID"
        android:layout_alignStart="@+id/nameID"
        android:layout_centerHorizontal="true"
        android:width="320dp"
        android:hint="Age" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/WHATWETHINK"
        android:id="@+id/infoID"
        android:onClick="pressWWT"
        android:background="#720101"
        android:textColor="#ffffff"
        android:layout_alignTop="@+id/buttonID"
        android:layout_alignEnd="@+id/ageID"
        android:layout_marginEnd="31dp" />

</RelativeLayout>
